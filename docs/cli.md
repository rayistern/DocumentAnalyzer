# DocumentAnalyzer CLI

This document describes the command-line interface for the DocumentAnalyzer tool.

## Global Options

These options apply to all commands:

- `--log-prompts`: Print the full prompt messages sent to the LLM API. Useful for debugging and understanding what's being sent to the model.

## Commands

### process

Process a single text file through OpenAI API. 